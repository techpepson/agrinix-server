<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Verification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background: #f9f9f9;
        color: #222;
      }
      .container {
        max-width: 500px;
        margin: 40px auto;
        background: #fff;
        padding: 32px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.07);
        text-align: center;
      }
      .logo {
        width: 160px;
        max-width: 80%;
        height: auto;
        margin-bottom: 24px;
        display: block;
        margin-left: auto;
        margin-right: auto;
      }
      .code {
        font-size: 1.2em;
        background: #f1f1f1;
        padding: 8px 16px;
        border-radius: 4px;
        display: inline-block;
        margin-top: 12px;
        letter-spacing: 2px;
      }
      .btn {
        display: inline-block;
        padding: 14px 32px;
        background: #28a745;
        color: #fff !important;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
        font-size: 1.1em;
        margin-top: 24px;
        box-shadow: 0 2px 6px rgba(40, 167, 69, 0.15);
        transition: background 0.2s;
      }
      .btn:hover {
        background: #218838;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- Logo Header -->
      <img
        src="https://res.cloudinary.com/dqriuqty5/image/upload/v1751628446/agrinix-logo_tlyhfm.png"
        alt="Agrinix Logo"
        class="logo"
      />
      <h2>Email Verification</h2>
      <p>Hello <%= userName %>,</p>
      <p>
        Thank you for registering on the Agrinix app. Please verify your email
        address to activate your account. Please note email token together with
        account will be deleted after 24 hours if email is not verified.
      </p>
      <p>Your verification code is:</p>
      <div class="code"><%= verificationId %></div>
      <p>Or click the button below to verify:</p>
      <a class="btn" href="<%= verificationUrl %>" target="_blank"
        >Verify Email</a
      >
      <p style="margin-top: 32px; color: #888; font-size: 0.95em">
        If you did not request this, you can safely ignore this email.
      </p>
      <p
        style="
          margin-top: 16px;
          color: #bbb;
          font-size: 0.85em;
          text-align: center;
        "
      >
        &copy; <%= new Date().getFullYear() %> Agrinix. All rights reserved.
      </p>
    </div>
  </body>
</html>
